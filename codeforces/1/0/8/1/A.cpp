#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int v;
    cin >> v;
    if (v != 2) {
        cout << 1;
    } else {
        cout << 2;
    }
    return 0;
}